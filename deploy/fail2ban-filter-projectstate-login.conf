[Definition]
# failed logins render the form again with HTTP 400 (POST /login, /signup, /admin/login, /oauth/login)
failregex = ^<HOST> .* "POST /(login|admin/login|oauth/login) HTTP/[0-9.]+" 400 
ignoreregex =
